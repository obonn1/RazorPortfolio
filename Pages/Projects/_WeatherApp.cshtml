@model ProjectsModel
@{
    ViewData["Title"] = "Weather";
}

<div class="col">
    <form class="form-inline" method="get">
        <div class="input-group w-75">
            <input asp-for="CityWeather" type="text" class="form-control mb-2 mr-sm-2" id="cityName" placeholder="City or Zip Code" />
            <button type="submit" class="btn btn-primary mb-2" value="submit" asp-page-handler="SubmitWeather">Submit</button>
        </div>
    </form>
</div>
<div class="row">
    @if (Model.Weather is null)
    {
        <p>See the current weather in any city!</p>
    }
    else
    {
        <p>
            <h4 class="card-title">@Model.Weather.Location</h4>
            <br />
            <div class="card-body">
                Temperature = @Model.Weather.Temperature
                <br />
                Humidity = @Model.Weather.Humidity
                <br />
                Condition = @Model.Weather.Condition

            </div>
        </p>
    }
</div>